<h1>New artifact_note</h1>

<% form_for(@artifact_note) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.label :ts %><br />
    <%= f.datetime_select :ts %>
  </p>
  <p>
    <%= f.label :note %><br />
    <%= f.text_area :note %>
  </p>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', artifact_notes_path %>